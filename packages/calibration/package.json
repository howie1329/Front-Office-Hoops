{
  "name": "@workspace/calibration",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "market-calibration": "vitest run --config vitest.market.config.ts",
    "sensitivity-baseline": "vitest run --config vitest.baseline.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@workspace/domain-v2": "*",
    "@workspace/league-schema": "*",
    "@workspace/sim-v2": "*"
  },
  "devDependencies": {
    "typescript": "^6",
    "vitest": "^3.2.4"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
